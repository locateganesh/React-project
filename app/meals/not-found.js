// reserved name.
// show when url mis-typed.
// NotFoundPage also a reserved name
export default function NotFoundPage() {
    return <main className="not-found">
        <h1>Meal not Found</h1>
        <p>Unfortunatly, We cannot found the meal, which you have requested.</p>
    </main>
}
// reserved name.
// show when url mis-typed.
// NotFoundPage also a reserved name
export default function NotFoundPage() {
    return <main className="not-found">
        <h1>Not Found</h1>
        <p>Unfortunatly, We cannot found the page, which you have requested.</p>
    </main>
}